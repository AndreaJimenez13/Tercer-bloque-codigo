Entrada de los números:

num1 = int(input("Ingresa el primer numero:"))
num1 = int(num1)
num2 = int(input("Ingresa el segundo numero:"))
El programa solicita al usuario ingresar dos números, los cuales se convierten a enteros usando int().

Condición 1: Si num1 es mayor que num2:

if (num1 > num2):
    print(str(num1) + "\nEs mayor que " + str(num2))
Si el primer número es mayor que el segundo, se imprime que num1 es mayor que num2.

Condición 2: Si num1 es igual a num2:

elif num1 == num2:
    print(str(num1) + "\nEs igual que " + str(num2))
Si los dos números son iguales, se imprime que ambos números son iguales.

Condición 3: Si num1 es menor que num2:


else:
    print(str(num1) + "\nEs menor que " + str(num2))
Si ninguna de las condiciones anteriores se cumple, significa que num1 es menor que num2, y el programa lo indica.

Mensaje de cierre:

print("¡Gracias por usar mi programa!")
Después de la comparación, el programa agradece al usuario por usarlo.
