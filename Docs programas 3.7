Lista de cadenas:


cadenas = ["Juan", "Pedro", "Chuy"]
print(cadenas)  # Imprime la lista completa
print(cadenas[1])  # Imprime el segundo elemento de la lista (índice 1: "Pedro")
print(type(cadenas))  # Imprime el tipo de la lista (será <class 'list'>)
print(type(cadenas[1]))  # Imprime el tipo del segundo elemento (será <class 'str'>, ya que es una cadena)
Se crea una lista cadenas con tres elementos de tipo cadena (strings).
Se accede al segundo elemento de la lista usando el índice 1.
Se imprime el tipo de la lista (list) y el tipo del segundo elemento (str).
Lista de enteros:


enteros = [5, 4, 8]
print(enteros)  # Imprime la lista completa
print(enteros[0])  # Imprime el primer elemento de la lista (índice 0: 5)
print(type(enteros))  # Imprime el tipo de la lista (será <class 'list'>)
print(type(enteros[0]))  # Imprime el tipo del primer elemento (será <class 'int'>, ya que es un entero)
Se crea una lista enteros con tres elementos de tipo entero (int).
Se accede al primer elemento de la lista usando el índice 0.
Se imprime el tipo de la lista (list) y el tipo del primer elemento (int).
Lista de valores booleanos:


booleanos = [True, False, False]
print(booleanos)  # Imprime la lista completa
print(booleanos[0])  # Imprime el primer elemento de la lista (índice 0: True)
print(type(booleanos))  # Imprime el tipo de la lista (será <class 'list'>)
print(type(booleanos[0]))  # Imprime el tipo del primer elemento (será <class 'bool'>, ya que es un valor booleano)
Se crea una lista booleanos con tres valores de tipo booleano (True y False).
Se accede al primer elemento de la lista usando el índice 0.
Se imprime el tipo de la lista (list) y el tipo del primer elemento (bool).
